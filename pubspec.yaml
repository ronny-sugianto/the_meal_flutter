name: the_meal_flutter
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_bloc: ^8.1.1

  # Networking & Data
  dio: ^4.0.6
  alice: ^0.3.2
  equatable: ^2.0.5

  # Misc
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

  # Unit Testing
  bloc_test: ^9.1.0
  mockito: ^5.3.2
  mocktail: ^0.3.0

  # Generator
  build_runner:

  # Integration Test
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true